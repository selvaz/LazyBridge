# Source   : lazy_wiki/human/tools.md
# Heading  : Enum constraints
# ID       : lazy_wiki/human/tools.md::enum-constraints::00
# Kind     : unknown
# Testable : skip
# Skip     : Contains ellipsis / placeholder — not complete code

from enum import Enum

class Format(str, Enum):
    JSON     = "json"
    MARKDOWN = "markdown"
    PLAIN    = "plain"

def export(content: str, fmt: Format = Format.JSON) -> str:
    """Export content in the given format."""
    ...

tool = LazyTool.from_function(export)
# fmt schema: {"enum": ["json", "markdown", "plain"]}
# The LLM can only pass one of those three strings
